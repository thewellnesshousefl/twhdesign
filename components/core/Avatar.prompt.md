A round avatar for practitioners and clients; shows an image or initials.

```jsx
<Avatar name="Dr. Maya Rivera" size={56} />
<Avatar src="/team/maya.jpg" name="Maya" />
```

`size` in px; `tone`: `sage` · `clay` · `stone`.
