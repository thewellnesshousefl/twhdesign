A small uppercase label that introduces a section, sitting above the heading. Includes a short leading rule.

```jsx
<Eyebrow>An elevated wellness experience</Eyebrow>
<h2>We meet you there</h2>
```

Tones: `sage` (default) · `muted` · `clay` · `light` (for dark grounds).
